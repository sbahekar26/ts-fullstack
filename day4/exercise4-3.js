/* Exercise 3 — Template literals.
Using the name and age variables from Exercise 1, 
log a single sentence like My name is X and I am Y years old. 
— built with a template literal, not +.*/

const name = "Sid";
const age = 27;
const likeCoffee = true;

console.log(`My name is ${name} and my age is ${age}.`);