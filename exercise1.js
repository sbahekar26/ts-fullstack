/* Exercise 1 — Variables & types.
Declare a const for your name (string), your age (number), and whether you like coffee (boolean). Then log all three, and log the typeof each.
Should print: three values, then string, number, boolean. */

const name = "Sid";
const age = 27;
const likeCoffee = true;

console.log(typeof name);
console.log(typeof age);
console.log(typeof likeCoffee);